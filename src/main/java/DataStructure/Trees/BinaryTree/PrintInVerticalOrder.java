package DataStructure.Trees.BinaryTree;



 class BinaryTree {
/*
  class Node{
  int data;
  Node left, right;

  Node(int item)
  {
    data = item;
    left = right = null;
  }
}

class Values

  {
    private  int minDist =0;
    private  int maxDist = 0;
  }

  Node root;
  Values val = new Values();

  private static void printVerticalOrder(Node head) {

    findMinMax(head,0,0);
    System.out.println(minDist+" "+maxDist);

  }

  private static void findMinMax(Node head, int min, int max) {
   if(head == null)
   {
     return;
   }
   if(minDist > min)
   {
     minDist = min;
   }
   if(maxDist < max )
   {
     maxDist = max;
   }
   findMinMax(head.left, min-1,max);
   findMinMax(head.right, min,max+1);

  }

  public static void main(String args[])
  {
    BinaryTree tree = new BinaryTree();

     //Let us construct the tree shown in above diagram
    tree.root = new Node(1);
    tree.root.left = new Node(2);
    tree.root.right = new Node(3);
    tree.root.left.left = new Node(4);
    tree.root.left.right = new Node(5);
    tree.root.right.left = new Node(6);
    tree.root.right.right = new Node(7);
    tree.root.right.left.right = new Node(8);
    tree.root.right.right.right = new Node(9);

    System.out.println("vertical order traversal is :");
    tree.verticalOrder(tree.root);
  }
*/

}
